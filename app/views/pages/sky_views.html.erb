<div class = "container narrow margin-top-height-navbar">
  <h1 class = "text-center"><%= t('gallery.sky_views') %></h1>
  <%= render "shared/gallery-links" %>
  <div class="row">
    <% @idvideos.each do |id| %>
      <div class="col-xs-12 col-sm-6 col-md-4 col-sm-3">
        <div class="cl-image-container">
          <div class="cl-image-pages">
            <%= video_tag("http://res.cloudinary.com/dsosadxbk/video/upload/v1491490317/"+id+".mp4", autoplay: true, muted: true, loop: true, class: "cl-image-pages") %>
          </div>
        </div>
      </div>
    <% end %>
    <% @ids.each do |id| %>
      <div class="col-xs-12 col-sm-6 col-md-4 col-sm-3">
        <div class="cl-image-container">
          <%= cl_image_tag(id, class: "cl-image-pages") %>
        </div>
      </div>
    <% end %>
  </div>
  <%= link_to gallery_path do %>
    <div class="back-gallery-button">
      <i class="fi-rewind"></i> <%= t('gallery.back') %>
    </div>
  <% end %>
</div>
